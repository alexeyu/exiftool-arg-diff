# exiftool-arg-diff

Diff old/new metadata into the minimal `exiftool` CLI args needed to apply
the change. Compute-only: this library never spawns `exiftool` itself.

Status: early scaffolding, no public API yet.
